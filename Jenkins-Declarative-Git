pipeline{
    agent any
    environment{
        PATH = "/opt/maven3/bin:$PATH"
    }
    stages{
        stage(){
            steps{
                git 'https://github.com/ybsgit/JavaHelloWorld'
            }
        }
        stage("maven build"){
            steps{
                sh "mvn clean package"
            }
        }
    }
}